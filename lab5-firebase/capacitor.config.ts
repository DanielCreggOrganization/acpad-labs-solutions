import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lab5-firebase',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
